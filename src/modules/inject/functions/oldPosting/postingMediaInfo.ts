const postingMediaInfo = () => {
	const mediaInfo = document.createElement('div');
	mediaInfo.id = 'media_info';
	mediaInfo.classList.add('media_info');

	return mediaInfo;
};

export default postingMediaInfo;